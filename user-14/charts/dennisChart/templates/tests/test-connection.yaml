apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dennisChart.fullname" . }}-test-connection"
  labels:
    {{- include "dennisChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dennisChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
