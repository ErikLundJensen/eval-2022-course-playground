apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user15Chart.fullname" . }}-test-connection"
  labels:
    {{- include "user15Chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user15Chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
